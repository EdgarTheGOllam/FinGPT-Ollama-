# FinGPT Requirements - Updated Version
# Generated from full Import Scan and Architectural Analysis
# Stand: 4. März 2026
# Minimum Requirement: Python 3.10+ (Recommended)

# ==============================================================================
# KERN-ABHÄNGIGKEITEN (Pflicht)
# =============================================================================

# HTTP-Anfragen (Ollama, OpenAI, APIs, httpx für async)
requests>=2.31.0
httpx>=0.26.0

# Datenverarbeitung & Numerik
numpy>=1.24.0
pandas==2.*
pandas_ta>=0.3.14b0  # Für technische Indikatoren
quantstats>=0.0.62  # Für professionelle Backtest-HTML-Reports

# Visualisierung
matplotlib>=3.7.0
mplfinance>=0.12.9

# Systeminformationen (CPU, RAM, Prozesse)
psutil>=5.9.0

# Bildverarbeitung (Icons, Assets)
Pillow>=10.0.0

# Umgebungsvariablen (.env Datei-Support)
python-dotenv>=1.0.0

# Web- & Dashboard-Komponenten
streamlit>=1.28.0
plotly>=5.18.0
feedparser>=6.0.0

# GUI Framework
customtkinter>=5.2.2

# Graphen und Netzwerkanalyse (Debate-Tab)
networkx>=3.1

# Broker-Schnittstelle (WICHTIG: Abhängig von MT5 Installation)
# MetaTrader5 >= 5.0.45
# Hinweis: Dies ist kritisch für mt5_broker.py und die meisten Trading-Module.
# MetaTrader5

# ================================================================================
# MACHINE LEARNING / REINFORCEMENT LEARNING (GPU-Unterstützung empfohlen)
# ================================================================================

# PyTorch (RL-Training, GPU-Unterstützung)
# Für CUDA-Support: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
stable-baselines3>=2.0.0
gymnasium>=0.29.0
optuna>=3.4.0  # Hyperparameter-Optimierung für RL
# Hinweis: Sicherstellen, dass die richtige PyTorch-Version für das System installiert ist.

# ================================================================================
# OPTIONAL: Web-Bridge / API-Server
# ================================================================================

# Flask Web-Framework (für externe Bridge/API)
# flask>=3.0.0
# flask-socketio>=5.3.0

# ================================================================================
# OPTIONAL: MT5 Bridge (Nur falls MT5 nicht über MetaTrader5-Paket verfügbar ist)
# ================================================================================

# Hinweis: Die Abhängigkeit sollte primär durch das MetaTrader5-Paket abgedeckt werden.
# Falls spezifische Funktionen des Bridges benötigt werden, müssen diese geprüft werden.

# ==============================================================================
# STDLIB (keine Installation nötig, zur Dokumentation)
# ==============================================================================
# tkinter, json, pathlib, threading, queue, datetime, logging, sqlite3, subprocess, os, sys, re, collections, dataclasses, typing, hashlib, hmac, pickle, random, signal, urllib, calendar, glob, atexit, functools, warnings, traceback

# ================================================================================
# API SERVER (FastAPI, Uvicorn)
# ================================================================================
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
